@isTest
private class Test_PackageScripts {
/*	
	@isTest 
	static void testOnInstall() {
		PackageScripts ps = new PackageScripts();
		Test.testInstall(ps, null);
		Test.testInstall(ps, new Version(1,0));
		
		Test.testUninstall(new PackageScripts());

	} */
	
}